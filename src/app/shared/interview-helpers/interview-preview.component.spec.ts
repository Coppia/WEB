/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InterviewPreviewComponent } from './interview-preview.component';

describe('Component: InterviewPreview', () => {
  it('should create an instance', () => {
    let component = new InterviewPreviewComponent();
    expect(component).toBeTruthy();
  });
});
