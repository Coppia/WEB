/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SnippetPreviewListComponent } from './snippet-preview-list.component';

describe('Component: SnippetPreviewList', () => {
  it('should create an instance', () => {
    let component = new SnippetPreviewListComponent();
    expect(component).toBeTruthy();
  });
});
